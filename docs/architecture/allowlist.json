{
  "typescript": {
    "apps/desktop/electron/main/bootstrap/window.ts": "Existing window lifecycle extraction from the main-process facade; further decomposition is outside this follow-up scope.",
    "packages/agent-runtime/src/native-pi-session.test.ts": "Native Pi continuation regression coverage is kept with the source boundary while the protocol and fixture contract stabilise; split by lifecycle once the native adapter surface settles.",
    "packages/agent-runtime/src/native-pi-session.ts": "Native Pi discovery, immutable projection, guarded continuation, and fork publication share one sidecar-private source boundary; split by lifecycle once the native adapter contract stabilises without widening path ownership."
  },
  "rust": {
    "crates/host-core/src/rpc/mod.rs": "Legacy RPC dispatch module; its extraction is outside this refactor scope.",
    "crates/host-core/src/sessions.rs": "Legacy session service module; its extraction is outside this refactor scope.",
    "crates/host-core/src/tools/mod.rs": "Legacy builtin tool dispatch module; its extraction is outside this refactor scope.",
    "crates/host-core/src/plugin_sessions.rs": "Legacy plugin session lifecycle module; its extraction is outside this refactor scope.",
    "crates/host-core/src/transcripts.rs": "Legacy transcript persistence module; its extraction is outside this refactor scope.",
    "crates/host-core/src/plugins/tests.rs": "Existing plugin-domain test module retained while production plugin code is split.",
    "crates/host-core/src/db/tests.rs": "Existing database-domain test module retained while production database code is split.",
    "crates/host-core/src/providers/tests.rs": "Existing provider-domain test module retained while production provider code is split.",
    "crates/host-core/src/user_skills.rs": "User-skill catalog crossed the Rust 1000-LOC limit when the scan-and-batch-import path landed (PR #515); the file still holds one cohesive discovery/import lifecycle and is exempted until the batch-import surface is separated.",
    "crates/host-core/src/config_sync/engine.rs": "Configuration-sync orchestration keeps capture, reconciliation, approval gating, journal recovery, and public state projection in one lifecycle; transport, domain capture, remote history, and apply responsibilities are already separated, and the remaining orchestration will be split after the sync lifecycle stabilises."
  }
}
