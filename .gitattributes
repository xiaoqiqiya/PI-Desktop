* text=auto eol=lf

# Generated diffs encode an empty context line as one significant space.
# Keep other whitespace diagnostics enabled; source-file checks are unchanged.
patches/*.patch whitespace=-blank-at-eol
